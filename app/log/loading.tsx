/**
 * Log screen skeleton — covers the §17 loading state until the server
 * component resolves.
 */
export default function LoadingLog() {
  return (
    <main className="column">
      <h1 className="py-lg font-display text-h1 text-ink">Log</h1>
      <p className="text-body text-muted">Loading…</p>
    </main>
  );
}
