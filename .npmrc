# Native build scripts (esbuild / sharp / unrs-resolver) are approved via the
# `onlyBuiltDependencies` list in pnpm-workspace.yaml. Skip pnpm's pre-script
# dependency re-check so a gate command (typecheck / lint / test / build) does
# not re-trigger `pnpm install`.
verify-deps-before-run=false
